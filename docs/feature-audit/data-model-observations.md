# Data Model Observations — m-friday.vercel.app

**Not populated.** See `README.md` — `BROWSER_ACCESS = NOT_AVAILABLE` for this target. No API
response, form field, or rendered data table was observed, so no entity/field shape can be
reported here without guessing. See README.md §3 to unblock (a source repo, if available, would
let this be filled in from real model/type definitions instead of inference from the UI).
