# Screenshots — m-friday.vercel.app

Empty on purpose. `BROWSER_ACCESS = NOT_AVAILABLE` for this target (see `../README.md`), so no
screenshot of the live site was captured — and none was fabricated to fill this folder. See
`../README.md` §3 for how to provide real screenshots or unblock live capture.
