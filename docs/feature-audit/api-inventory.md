# API Inventory — m-friday.vercel.app

**Not populated.** See `README.md` — `BROWSER_ACCESS = NOT_AVAILABLE` for this target, so no
network traffic to/from the site could be observed (no DevTools/network tab access, since the
site itself was never reachable to open).

| API endpoint | HTTP method | Request | Response | Authentication | Source | Observed behavior |
|---|---|---|---|---|---|---|
| _(none yet)_ | | | | | | |

The most direct way to fill this in without needing this session to reach the target is a HAR
export from your own browser session on the site (see README.md §3, option 4) — I can parse a HAR
file you provide without needing network access myself.
