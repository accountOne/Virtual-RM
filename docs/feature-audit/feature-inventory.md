# Feature Inventory — m-friday.vercel.app

**Not populated.** This audit could not reach the target (`https://m-friday.vercel.app`) — see
`README.md` in this directory for the full browser-capability check and evidence
(`BROWSER_ACCESS = NOT_AVAILABLE` for this target). No feature was observed, so none is listed
below. Listing feature names guessed from the site's title or from this project's own BRD would
be speculation, which the audit task explicitly disallows ("Không tự suy đoán các tính năng chưa
kiểm tra").

Once real access is available (see README §3 for options), this table should be filled in as:

| ID | Feature | Screen | Description | Entry point | Status | Evidence |
|---|---|---|---|---|---|---|
| _(none yet)_ | | | | | | |

Status legend: `WORKING` / `PARTIAL` / `MOCK` / `BROKEN` / `NOT_TESTED` — each row's Status must
be backed by an actual observation (screenshot, DOM read, or network call), not inferred from a
menu label or from source code alone.
