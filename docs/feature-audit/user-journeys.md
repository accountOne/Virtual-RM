# User Journeys — m-friday.vercel.app

**Not populated.** See `README.md` — `BROWSER_ACCESS = NOT_AVAILABLE` for this target. No
journey (login, dashboard, transfer, Virtual RM interaction, etc.) was actually walked through,
so none is listed — per the task's own instruction, a journey is only added "khi thực sự phát
hiện được trong ứng dụng" (when actually discovered in the app). Pre-listing the example journey
IDs from the task prompt (J-001 Login, J-002 Dashboard, ...) without having observed them would
be guessing.

Template for when real access is available:

```
Journey ID:
Journey name:
Actor:
Precondition:
Steps:
Expected result:
Actual result:
Status:
Screenshots:
Issues:
```

See README.md §3 to unblock.
